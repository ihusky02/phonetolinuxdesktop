<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.openxmlformats.org/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" 
        x:Class="phonetolinux.Views.IpConfigWindow"
        Title="Phone Connection Setup"
        Width="380" Height="200"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        SystemDecorations="BorderOnly"
        Background="#121212">
    
    <StackPanel Margin="20" VerticalAlignment="Center" Spacing="12">
        <TextBlock Text="Write IP Adress:" 
                   Foreground="White" 
                   FontSize="14" 
                   FontWeight="SemiBold"/>
        
        <TextBox x:Name="IpTextBox" 
                 Watermark="np. 192.168.X.XX" 
                 Background="#2C2C2C" 
                 Foreground="White" 
                 CaretBrush="Black"
                 BorderBrush="#444444"
                 CornerRadius="8"
                 Height="35"/>

        <Button Content="Connect/Save" 
                Background="#1E88E5" 
                Foreground="White" 
                CornerRadius="8"
                Height="38"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center"
                VerticalContentAlignment="Center"
                Click="Save_Click"/>
    </StackPanel>
</Window>